// Examples of converting text string to numbers
const integerNumber = '20';
console.log(Number.parseInt(integerNumber));

const floatingNumber = '20.2';
console.log(Number.parseFloat(floatingNumber));

// Check if the value is truly an integer
const number = 20;
console.log(Number.isInteger(number)); // true
