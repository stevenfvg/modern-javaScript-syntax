const fullName = 'Nicolas Kim Coppola';
const email = 'Nicolas.kim@email.com';

// .toUpperCase() methods to transform the text of the string into uppercase letters
console.log(`name: ${fullName.toUpperCase()}`);
// .toLowerCase() method to transform the text of the string into lowercase letters.
console.log(`email: ${email.toLowerCase()}`);
